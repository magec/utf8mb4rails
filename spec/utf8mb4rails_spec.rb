require 'spec_helper'

describe Utf8mb4rails do
  it 'has a version number' do
    expect(Utf8mb4rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
